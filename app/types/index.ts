// User types
export interface User {
    id: string;
    email: string;
    fullName: string;
    program: string;
    yearLevel: string;
    profilePhotoUrl?: string;
}

// Navigation types will be added later